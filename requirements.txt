# ROCKET — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core deep learning ────────────────────────────────────────────────────
torch==1.12.0
torch-geometric==2.3.0
torchvision==0.13.0

# ── EHR / clinical data ───────────────────────────────────────────────────
pyhealth==1.1.2

# ── Scientific computing ─────────────────────────────────────────────────
numpy>=1.23.0
scipy>=1.9.0
scikit-learn>=1.2.1
pandas>=1.5.0
networkx>=3.0

# ── OpenAI (KG generation + embeddings) ──────────────────────────────────
openai==0.27.4

# ── Causal discovery (optional — install for PC and LiNGAM support) ──────
# causal-learn>=0.1.3.8    # PC algorithm
# lingam>=1.8.2             # DirectLiNGAM

# ── Configuration ─────────────────────────────────────────────────────────
pyyaml>=6.0

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=7.2.0
pytest-cov>=4.0.0

# ── Notebooks ─────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
matplotlib>=3.6.0
seaborn>=0.12.0

# ── Logging / experiment tracking (optional) ─────────────────────────────
# neptune-client>=1.0.0     # pip install neptune-client if using Neptune
